version: "3.9"

services:
  app:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: unless-stopped

    # DEV ONLY (optional, enables live reload)
    volumes:
      - .:/app

    command: uv run uvicorn main:app --host 0.0.0.0 --port 5000 --reload